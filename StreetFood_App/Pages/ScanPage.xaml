<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="StreetFood_App.Pages.ScanPage"
             Title="Quét mã QR"
             BackgroundColor="Black">

    <Grid>
        <zxing:CameraBarcodeReaderView x:Name="MyCamera"
                                       BarcodesDetected="Camera_BarcodesDetected"
                                       IsDetecting="True"
                                       IsTorchOn="False"/>

        <Border Stroke="Red"
                StrokeThickness="3"
                HeightRequest="250"
                WidthRequest="250"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                BackgroundColor="Transparent"/>

        <Label Text="Di chuyển mã QR vào khung đỏ để nghe thuyết minh"
               TextColor="White"
               FontSize="16"
               HorizontalOptions="Center"
               VerticalOptions="End"
               Margin="0,0,0,120"
               HorizontalTextAlignment="Center"/>

        <Button Text="⚡ [TEST] Giả vờ đã quét trúng"
                BackgroundColor="Yellow"
                TextColor="Black"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                VerticalOptions="End"
                Margin="0,0,0,60"
                Clicked="OnFakeScanClicked"/>

        <Button Text="Đóng"
                BackgroundColor="#88000000"
                TextColor="White"
                HorizontalOptions="Start"
                VerticalOptions="Start"
                Margin="20"
                Clicked="OnCloseClicked"/>
    </Grid>
</ContentPage>